{
  "name": "todo-mern-google-auth",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "vercel-build": "npm --workspace client run build"
  },
  "engines": {
    "node": ">=18"
  }
}

